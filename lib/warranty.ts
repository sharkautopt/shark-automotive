// Garantia is a fixed constant, not a per-vehicle field — stock vehicles only,
// never mentioned in the custom-order/"encomenda" flow. Exact legal wording,
// never abbreviated. Single source of truth, reused by the public site and
// by DOC-04 (window sticker) — the one PDF this term applies to.
export const WARRANTY_TERM = '18 meses por mútuo acordo'
export const WARRANTY_LABEL = `Garantia de ${WARRANTY_TERM}.`
